<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        h3{text-align: center;}
    </style>
    <title><%= title %></title>
</head>
<body>
    <%- include('heading.ejs') %>
    <div class="my-6 mx-10">
        <h3 class="text-4xl my-4 font-semibold">Welcome <%= userName %></h3>
        <div class="flex gap-2 py-4 justify-between">
            <h5 class="text-2xl">Elections <span class="bg-gray-100 ml-2 px-2"><%= noOfElections %></span></h5>
            <div class="bg-black rounded w-fit px-2 py-1 hover:bg-black">
                <a href="/electionpage/addelection" class="text-white">Create election</a>
            </div>
        </div>
        <div class="flex-auto">
            <div class="mx-4">
                <p class="text-purple-600"><%= (listOfElections.length==0)?"You don't have elections yet!":"" %></p>
                <ol class="list-decimal">
                    <% for (let i=0; i<listOfElections.length; i++) { %>
                        <li>
                            <div class="flex items-center w-fit my-2 px-2 py-1 ">
                                <a href="/electionpage/<%= listOfElections[i].id %>" class="m-2 text-sm text-black "><%= listOfElections[i].electionName %></a>
                            </div>
                        </li>
                    <% } %>
                </ol>
            </div>
        </div>
    </div>
    

</body>
</html>